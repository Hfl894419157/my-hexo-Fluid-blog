# 网站内容后台使用说明

本网站使用 Pages CMS 编辑 GitHub 仓库中的 Markdown。后台只是编辑界面，内容、图片、历史版本和部署仍由 GitHub 管理。

## 首页视频案例

首页视频支持哔哩哔哩和香港 OSS 两种来源。视频源文件不会进入网站仓库；后台只保存 B 站链接或 OSS 播放地址。

1. 进入 Pages CMS 的“站点管理 → 首页视频案例”。
2. 在“视频来源”选择“哔哩哔哩”或“OSS 视频”。
3. 哔哩哔哩模式填写包含 `BV` 编号的完整链接；OSS 模式直接选择不超过 500MB 的 H.264/AAC MP4，等待上传完成。
4. 新增或展开一条案例，填写标题、类型、说明和时长，并确认所选视频来源已就绪。
5. 在“首页封面”上传 16:9 图片，建议至少 1600×900。
6. 打开“在首页显示”，拖动条目调整顺序，保存即可触发重新部署。

注意：

- B 站分享生成的 `b23.tv` 短链接如果不包含 `BV` 编号，请先在浏览器打开短链接，再复制跳转后的完整视频地址。
- 最多展示 4 条；列表顺序就是首页顺序。只有 1 条时不显示缩略列表，配置 2 条及以上时显示全部切换缩略图。
- “视频封面与缩略图”用于主画面封面；多条视频时也用于下方切换卡，不能留空。
- B 站视频继续在新标签页打开；OSS 视频在首页卡片内按需播放，不会在访客点击前下载完整视频。
- OSS 视频上传中、暂停或失败时后台会阻止保存，避免发布无效地址。
- 未打开“在首页显示”、缺少封面或链接无法识别的条目不会出现在首页。
- 更换为新的 B 站稿件时只需替换链接；修改标题、说明或封面直接在 Pages CMS 中完成。

## 第一次登录

1. 打开 <https://app.pagescms.org/>。
2. 使用 GitHub 登录。
3. 安装 Pages CMS GitHub App 时，只授权 `han-yujie/hanyujie.github.io` 仓库。
4. 打开仓库的 `main` 分支，后台会读取根目录中的 `.pages.yml`。

## 新增详情内容

1. 进入“作品集”“工作流”，或展开“知识库内容”后进入“研究笔记”“方法指南”“工具与资源”。
2. 点击新增后直接填写文章内容。系统会根据标题自动生成“拼音摘要 + 时间短码”的稳定文件名，不需要手工填写或修改。
3. 依次填写“基本信息、发布设置、卡片与封面、内容模块、高级设置”。不再单独维护 `slug`。
4. 初次保存使用“草稿”；想提前显示占位卡时使用“筹备中”；完成后改为“已发布”。
5. “已归档”会保留文件与历史，但不再公开显示。

发布状态规则：

- `draft`：不进入公开页面、卡片、搜索、侧栏和站点地图。
- `planned`：总览显示“内容筹备中”卡片，但详情页不公开。
- `published`：总览卡片可点击，详情页公开。
- `archived`：保留内容但从网站隐藏。

## 复制与删除文章

五个内容栏目中的文章都允许复制和删除；已发布文章沿用现有地址，不在后台开放手工重命名。

- 编辑文章时可点击顶部“复制为新草稿”，只需输入新标题。系统会自动生成新文件名，保留正文与封面，重新生成文章及模块编号，并强制创建为不公开草稿。
- 编辑页面不再显示文件名，也不提供手工重命名，避免标题修改或误操作影响已发布链接。历史上已经产生的旧网址跳转仍会保留。
- 条目菜单和编辑页提供“Delete”。确认后只删除当前 Markdown 文章，旧网址返回 404，文章会自动从首页、栏目、搜索、侧栏和站点地图中消失。
- 删除文章不会删除 `public/images/uploads/` 中的任何图片。文章和删除记录仍保留在 Git 历史中，需要时可以恢复。

## 管理首页精选

进入“站点管理 → 首页内容”：

- “首页作品精选”最多选择 2 条；列表顺序就是首页静态作品顺序。
- “精选工作流”最多选择 3 条，固定使用三列案例卡；移动端自动改为单列。
- “研究笔记、方法指南、工具与资源”各最多选择 3 条；没有选择真实内容时，首页不会显示空知识板块。
- 作品和知识精选只接受“已发布”内容。工作流精选允许选择“已发布”或“筹备中”，因此可以先保留第三张筹备卡，再到“工作流”集合中完善内容。
- 首页保存的是文章的隐藏唯一编号，因此修改 `.md` 文件名后精选关系仍会保留；已经删除或缺失的旧引用会被安全忽略并提示，不会拖垮整站部署。

仓库已经提供“新工作流案例待补充”筹备条目。可在 Pages CMS 的“工作流”中直接修改封面、标题、摘要、标签与正文，完成后将状态改为“已发布”。新内容默认不进入首页，必须在这里主动选择。

## 最近维护更新

已发布内容默认计入首页“网站最近维护更新”。系统根据该 Markdown 在 GitHub 中的最后提交时间，自动显示最近 6 条。

如果只是修正错字、不希望它进入最近更新，请关闭“计入首页最近更新”后再保存。

## 图片

封面、正文图片和作品图片组都可以在后台直接上传或粘贴。新图片会按内容唯一编号自动归档到：

```text
public/images/uploads/content/{contentId}/
```

不需要为每篇文章手工创建文件夹，也不需要先进入媒体库上传再引用。旧的 `imported` 和历史目录继续保留并可正常选择，不做批量迁移。

正文和卡片使用的公开地址会自动写成：

```text
/images/uploads/文件名
```

保存后，构建会自动为 PNG、JPG、JPEG 和普通 WebP 生成 320、480、760、1080px 及原图宽度的响应式 WebP。原图继续保留给 Pages CMS 编辑，衍生文件只进入部署产物；不需要在后台手工压缩。

可以在 GitHub 单独删除图片，但这样会绕过 Pages CMS 的引用检查，仍被文章或作品引用时会直接造成破图。正常流程是先移除所有内容引用，再从 Pages CMS 媒体库删除。Git 历史中仍可恢复原图；OSS 中已不再引用的 WebP/AVIF 衍生文件目前不会自动清理。

本站继续采用“GitHub 保存原图、OSS 承载大部分自动生成衍生图”的单一媒体流程，不把原图再上传一份到 OSS。这样文章、封面和作品图片都能直接在 Pages CMS 中选择和维护，避免两套媒体库的路径与删除规则互相冲突。

封面、封面替代文字、SEO 标题、SEO 描述和标签都可以留空。封面替代文字为空时网站使用文章标题，SEO 为空时使用文章标题和卡片摘要，标签为空只显示提醒。已发布文章仍必须填写标题、卡片摘要、创建日期、发布状态，并至少保留一个内容模块。

## 内容模块

作品集使用两类模块：“正文说明”和“作品图片组”；工作流及知识内容使用统一“正文”。模块可以新增、排序、折叠和删除，并由后台保存隐藏编号。

- “正文说明 / 正文”：使用富文本编辑器维护段落、标题、列表、引用、代码、图片及视频或网盘链接，也可以切换 HTML Source。
- “作品图片组”：仅作品集提供，可添加多张图片并拖动排序。

正文图片、图片组和卡片封面统一生成响应式 WebP/AVIF。首页视频案例独立在“站点管理 → 首页视频案例”维护，不需要在正文模块重复配置。

## 识别视频与网盘信息

托管版 Pages CMS 不在粘贴瞬间联网。正确顺序是：

1. 把视频或网盘分享文案粘贴到对应模块。
2. 先保存为草稿。
3. 点击条目右上角“识别内容模块”。
4. 选择识别方式并确认；识别提交完成后刷新条目，检查结果再发布。

三种识别方式：

- “仅补充空白”：默认且最安全，只补当前为空的字段。
- “更新自动字段”：只更新上次由系统生成、之后没有被人工修改的字段。
- “全部重新识别”：重新写入可自动识别字段，但永远不覆盖自定义封面。

识别不使用第三方 API 密钥。YouTube 与哔哩哔哩会尽量获取公开元数据；腾讯视频、优酷、抖音、小红书和视频号只生成外链卡片。网盘支持百度、阿里、夸克、123 云盘和蓝奏云。无法确认的信息保持为空；检测到多个链接时会停止写入并提示人工确认。

## 工具与资源

“工具与资源”文章需要选择资源类型和获取方式。第三方商业软件选择“访问官网”，自制或获准分享的文件选择“网盘下载”，需要人工确认的资料选择“联系获取”。不上传破解软件或来源不明的安装包。

## 关于我、个人资料、FAQ 与知识库入口

- “站点管理 → 关于我页面”维护首页关于我摘要、关于页首屏人物图、工作台能力选项及完整交付流程，列表顺序就是前台顺序。
- “站点管理 → 个人资料与联系方式”维护姓名、职业定位、头像、简介、实践年限、邮箱和简历 PDF。
- 已停用的“旧版关于页卡片”已经从后台移除，避免与当前关于页字段重复。
- “站点管理 → 常见问题 FAQ”按列表顺序维护合作咨询、资源下载和网站使用问题；关闭“前台显示”即可暂时隐藏问题。
- “站点管理 → 知识库总览”维护三个栏目入口的标题、说明和标签。

三个知识栏目本身都是多文章集合，可以持续新增文章；知识库首页会自动汇总最近发布的 6 篇内容。

## 使用 AI 生成可识别的正文

需要 AI 协助写正文时，可直接复制下面的话术：

````text
请输出可直接粘贴到 Pages CMS“正文 → 来源”中的标准 Markdown。

要求：
1. 只输出正文，不要输出 YAML、front matter、JSON、解释说明，也不要用一整层 ```markdown 包裹全文。
2. 文章标题由后台单独填写，正文从二级标题 ## 开始。
3. 可使用 ##/### 标题、普通段落、加粗、引用、项目列表、编号列表、表格、链接和代码块。
4. 段落之间保留一个空行，不使用复杂内联样式、脚本、iframe 或自定义组件。
5. 有真实图片网址时使用：![准确描述](https://完整图片网址)；没有真实网址时不要虚构图片地址，只写“此处建议插入：图片说明”。
6. 链接必须使用完整 HTTPS 地址。
7. 保持中文标点和清晰层级，直接给出最终正文。
````

固定使用方式：进入“正文 → 来源”，选择“自动识别（推荐）”，粘贴 AI 输出，再返回“编辑”检查排版。后台会把 Markdown 转换为安全 HTML，并自动归档可以访问的外部图片。

## 发布与回退

- Pages CMS 保存会形成 GitHub 提交，并触发内容检查与 VitePress 构建。
- 只有构建成功，GitHub Pages 才会更新。
- 临时撤下但希望保留后台条目时使用“草稿”或“归档”；确定不再需要时才删除。
- 如果发布内容有误，可在 GitHub 历史中回退对应提交。
